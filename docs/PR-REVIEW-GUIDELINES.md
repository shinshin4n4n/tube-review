# PRレビュー・コメントガイドライン

## 概要

このドキュメントは、ちゅぶれびゅ！プロジェクトにおけるPRレビューとコメントの標準的な手順を定義します。
一人開発における効率的なレビュープロセスを確立します。

---

## GitHubレビューの仕様

### レビュータイプ

GitHubには3種類のレビュータイプがあります：

| タイプ | コマンド | 用途 | 自己PRでの可否 |
|--------|---------|------|--------------|
| **Comment** | `gh pr review X --comment` | 意見を述べる | ✅ 可能 |
| **Approve** | `gh pr review X --approve` | 変更を承認する | ❌ **不可**（作成者本人） |
| **Request Changes** | `gh pr review X --request-changes` | 修正を要求する | ✅ 可能（意味はない） |

### 重要な制限事項

#### 自己Approveの禁止

```
⚠️ GitHubの仕様:
自分が作成したPRを自分でApproveすることはできません

理由:
- コードレビューの品質保証
- バイアスの排除
- チーム開発のベストプラクティス
```

**誤解されやすいポイント**:
- ❌ 「コメントを残すとApproveできなくなる」← 誤り
- ✅ 「作成者本人だからApproveできない」← 正しい

---

## 一人開発におけるレビューフロー

### 標準的な手順

```
1. PRを作成
   ↓
2. セルフレビュー実施
   ├─ GitHubにコメント投稿（要約）
   └─ docs/reviews/ にファイル作成（詳細）
   ↓
3. 直接マージ（Approveなし）
   ↓
4. （オプション）詳細レビューファイルをコミット
```

---

## レビューコメントの書き方

### Option A: GitHubコメントのみ（シンプル）

**用途**: 小規模な変更、シンプルなPR

**コマンド**:
```bash
gh pr review [PR番号] --comment --body "[レビュー内容]"
```

**例**:
```bash
gh pr review 13 --comment --body "## ✅ セルフレビュー完了

### Good Points
- 機能実装完了
- テスト全て合格

### テスト結果
- E2E: 6/6 passed
- Unit: 20/20 passed

一人開発のため直接マージします。"
```

**メリット**:
- ✅ PRページで直接見れる
- ✅ URLで共有可能
- ✅ 手順が簡単

**デメリット**:
- ❌ 長文だと読みにくい
- ❌ Markdownフォーマットが崩れる可能性

---

### Option B: docs/reviews/ ファイル（詳細）

**用途**: 大規模な変更、学びが多いPR

**ファイル構成**:
```
docs/reviews/pr-[番号]-review.md
```

**テンプレート**:
```markdown
# PR #[番号] レビュー: [タイトル]

## レビュー日時
YYYY-MM-DD

## レビュー結果
- ✅ Approve（一人開発のため直接マージ）

---

## Good Points（良い点）

1. **[カテゴリ]**: [詳細]
2. **[カテゴリ]**: [詳細]

---

## Suggestions（改善提案）

1. **[項目]**: [提案内容]
   - 優先度: High/Medium/Low
   - 対応: 今回/次回/将来

---

## Critical Issues（致命的な問題）

なし ✅

または

1. **[問題]**: [詳細]
   - 修正方法: [手順]

---

## Learning Points（学んだこと）

1. **[技術・概念]**: [学んだ内容]
2. **[技術・概念]**: [学んだ内容]

---

## 総合評価

### 受入基準達成度
- [要件1] → ✅
- [要件2] → ✅

### 品質スコア
- ドキュメント準拠: ⭐⭐⭐⭐⭐
- コード品質: ⭐⭐⭐⭐⭐
- テスト品質: ⭐⭐⭐⭐⭐

### 次のアクション
- ✅ マージ承認
- 🔜 次のタスクへ進む
```

**メリット**:
- ✅ 詳細な情報を記録できる
- ✅ Markdownフォーマットが保たれる
- ✅ Gitで管理される
- ✅ 将来的に見返しやすい

**デメリット**:
- ❌ PRページから直接見れない
- ❌ ファイルを開く必要がある

---

### Option C: ハイブリッド（推奨）✅

**用途**: すべてのPR（標準）

**手順**:

#### Step 1: GitHubに要約コメント
```bash
gh pr review [PR番号] --comment --body "## ✅ セルフレビュー完了

### Good Points
- [要点1]
- [要点2]

### テスト結果
- E2E: X/X passed
- Unit: X/X passed

詳細レビューは \`docs/reviews/pr-[番号]-review.md\` を参照

一人開発のため直接マージします。"
```

#### Step 2: 詳細レビューファイル作成
```
docs/reviews/pr-[番号]-review.md
（上記テンプレートを使用）
```

#### Step 3: マージ後にコミット（オプション）
```bash
git add docs/reviews/pr-[番号]-review.md
git commit -m "docs: Add PR #[番号] detailed review"
git push origin main
```

**メリット**:
- ✅ PRページで要約が見れる
- ✅ 詳細情報はファイルで保存
- ✅ 両方のメリットを享受

---

## レビューコメントのテンプレート

### 小規模なPR（1-2ファイル変更）

```markdown
## ✅ セルフレビュー完了

### Good Points
- [主要な良い点1つ]

### テスト結果
- すべて合格 ✅

一人開発のため直接マージします。
```

### 中規模なPR（3-10ファイル変更）

```markdown
## ✅ セルフレビュー完了

### Good Points
- [良い点1]
- [良い点2]
- [良い点3]

### テスト結果
- E2E: X/X passed ✅
- Unit: X/X passed ✅

### 変更内容
- [主要な変更1]
- [主要な変更2]

一人開発のため直接マージします。
```

### 大規模なPR（10+ファイル変更）

```markdown
## ✅ セルフレビュー完了

### Good Points
- [良い点1]
- [良い点2]
- [良い点3]

### テスト結果
- E2E: X/X passed ✅
- Unit: X/X passed ✅
- カバレッジ: XX% ✅

### 変更内容
- [主要な変更1]
- [主要な変更2]
- [主要な変更3]

### Learning Points
- [学んだこと1]
- [学んだこと2]

詳細レビューは \`docs/reviews/pr-[番号]-review.md\` を参照

一人開発のため直接マージします。
```

---

## マージ手順

### 標準的なマージフロー

```bash
# Step 1: レビューコメント投稿
gh pr review [PR番号] --comment --body "[レビュー内容]"

# Step 2: マージ実行
gh pr merge [PR番号] --squash --delete-branch

# Step 3: 確認
git checkout main
git pull origin main
gh issue view [Issue番号]  # 自動Closeされたか確認
gh pr view [PR番号]         # Mergedか確認

# Step 4: （オプション）詳細レビューファイルをコミット
git add docs/reviews/pr-[番号]-review.md
git commit -m "docs: Add PR #[番号] detailed review"
git push origin main
```

---

## 🚨 重要な原則（必読）

### ✅ 必須事項

1. **すべてのPRにレビューコメントを残す**
   - レビューコメントなしでマージは厳禁
   - 最低限でもGitHubのPRページにコメント投稿

2. **レビューコメント投稿のタイミング**
   - マージ「前」に必ず実施
   - テスト完了後、マージ実行前

3. **使用するコマンド**
   ```bash
   # 新規PRのレビュー
   gh pr review [PR番号] --comment --body "[レビュー内容]"

   # マージ済みPRへの追加（緊急時のみ）
   gh pr comment [PR番号] --body "[レビュー内容]"
   ```

4. **レビューコメントの最小構成**
   - Good Points（必須）
   - テスト結果（必須）
   - 変更内容（推奨）

### ⚠️ 禁止事項

1. **レビューコメントなしでマージ**
   ```
   ❌ 悪い例: テスト実行 → 即マージ
   ✅ 良い例: テスト実行 → レビューコメント投稿 → マージ
   ```

2. **事後的なレビューコメント追加（原則禁止）**
   - マージ後のコメントは記録が分散する
   - 例外: 過去のPRに対する大規模な修正時のみ

3. **レビューコメントの省略**
   - 「小さな変更だから」という理由で省略しない
   - すべてのPRは記録に値する

### 📋 標準フロー（必ず守る）

```bash
# Step 1: 実装完了
# ... コーディング ...

# Step 2: テスト実行・確認
npm run test
npm run build

# Step 3: ⚠️ レビューコメント投稿（マージ前に必須）
gh pr review [PR番号] --comment --body "## ✅ セルフレビュー完了

### Good Points
- [良い点1]
- [良い点2]

### テスト結果
- E2E: X/X passed ✅
- Unit: X/X passed ✅

### 変更内容
- [主要な変更1]
- [主要な変更2]

一人開発のため直接マージします。"

# Step 4: マージ実行
gh pr merge [PR番号] --squash --delete-branch

# Step 5: （オプション）詳細レビューファイルをコミット
# docs/reviews/pr-[番号]-review.md を作成・コミット
```

### 🎯 チェックリスト

PRをマージする前に、以下をすべて確認：

- [ ] テストがすべて合格している
- [ ] TypeScript型チェックが合格している
- [ ] ESLintが合格している
- [ ] ビルドが成功している
- [ ] **GitHubにレビューコメントを投稿した** ← 最重要
- [ ] Issue番号が正しくリンクされている（Closes #XX）

### 📝 レビューコメントの例

#### 最小限のレビューコメント
```markdown
## ✅ セルフレビュー完了

### Good Points
- 機能実装完了

### テスト結果
- すべて合格 ✅

一人開発のため直接マージします。
```

#### 推奨されるレビューコメント
```markdown
## ✅ セルフレビュー完了

### Good Points
- チャンネル検索UI実装完了
- TDD原則遵守（E2Eテスト先行）
- UI_DESIGN.mdのカラーパレット準拠

### テスト結果
- E2E Test: 8/8 passed ✅
- TypeScript型チェック: 合格 ✅
- ESLint: 合格 ✅
- Next.js Build: 成功 ✅

### 変更内容
- app/search/page.tsx: 検索ページ実装
- app/_components/search-form.tsx: 検索フォーム実装
- tests/e2e/search.spec.ts: E2Eテスト実装

一人開発のため直接マージします。
```

---

## ベストプラクティス

### ✅ 推奨されること

1. **すべてのPRにレビューコメントを残す**
   - 将来的に見返す価値がある
   - 学びの記録になる

2. **Good Pointsを必ず書く**
   - 自己肯定感の向上
   - 良いパターンの認識

3. **Learning Pointsを記録する**
   - 知識の定着
   - 将来の参考になる

4. **テスト結果を必ず記載する**
   - 品質の証明
   - デグレードの防止

5. **詳細レビューファイルをGit管理する**
   - 長期的な記録
   - プロジェクトの歴史

### ❌ 避けるべきこと

1. **Approveを試みる**
   - 自己PRでは不可能
   - エラーになる

2. **レビューコメントなしでマージ**
   - 記録が残らない
   - 学びの機会を逃す

3. **Critical Issuesを無視してマージ**
   - 品質の低下
   - 技術的負債の蓄積

4. **テスト結果を記載しない**
   - 品質の不透明化
   - 信頼性の低下

---

## トラブルシューティング

### Q1: 自己Approveしようとしてエラーになった

**エラー**:
```
Review Can not approve your own pull request
```

**解決**:
```
これはGitHubの仕様です。
Approveをスキップして、直接マージしてください。

gh pr merge [PR番号] --squash --delete-branch
```

---

### Q2: レビューコメントが長すぎて読みにくい

**解決**:
```
ハイブリッド方式を使用してください：
1. GitHubには要約のみ投稿
2. 詳細は docs/reviews/ に保存
```

---

### Q3: 詳細レビューファイルをどこに保存するか

**推奨**:
```
docs/reviews/pr-[番号]-review.md

例:
docs/reviews/pr-13-review.md
docs/reviews/pr-14-review.md
```

**理由**:
- 一貫性
- 見つけやすさ
- Git管理しやすい

---

## 将来的なチーム開発への移行

### チームメンバーが加わった場合

#### レビュープロセスの変更

```
現在（一人開発）:
作成者 → セルフレビュー → マージ

将来（チーム開発）:
作成者 → PR作成 → 他メンバーがApprove → マージ
```

#### ブランチ保護ルールの設定

**Settings > Branches > Branch protection rules**:

```
☑ Require a pull request before merging
  ☑ Require approvals (1)
☑ Require status checks to pass before merging
☐ Allow specified actors to bypass required pull requests
```

---

## まとめ

### 標準フロー（推奨）

```
1. PRを作成
   ↓
2. GitHubにレビューコメント投稿（要約）
   gh pr review X --comment --body "..."
   ↓
3. （オプション）docs/reviews/ に詳細レビュー作成
   ↓
4. 直接マージ（Approveなし）
   gh pr merge X --squash --delete-branch
   ↓
5. （オプション）詳細レビューファイルをコミット
```

### 重要な原則

1. ✅ すべてのPRにレビューコメントを残す
2. ✅ 自己Approveは不可（GitHubの仕様）
3. ✅ ハイブリッド方式を活用（要約 + 詳細）
4. ✅ 学びを記録する（Learning Points）
5. ✅ テスト結果を必ず記載

---

## 参考リンク

- [GitHub Docs: About pull request reviews](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/reviewing-changes-in-pull-requests/about-pull-request-reviews)
- [GitHub CLI: pr review](https://cli.github.com/manual/gh_pr_review)
- [Best Practices for Code Review](https://google.github.io/eng-practices/review/)

---

**最終更新日**: 2026-02-04